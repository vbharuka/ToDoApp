TableViewController
No delegates, no data source
table view Cell accessory -  checkmark

Navigation controller
Top bar controller
Setting background of navigation controller for all navigations and changing text color

Added Bar button and set the system item to add
In the click action, alert pops up to add new item
Alert Creation :-
Create UIAlertController
Create AlertAction and add to the alert(write blocks when add is pressed)
Add textfiled to the alert
Present alert


All data is defaults are loaded simultaneously before it can be used so it is better to not use it as data base
The data is defaults are stored in plist
We generally use UserDefaults.standard object to access user defaults

